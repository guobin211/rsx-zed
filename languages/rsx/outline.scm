; Outline/structure for RSX files

; Main sections
(rust_section) @item

(script_section) @item

(style_section) @item

(template_section) @item

; HTML elements with tag names
(html_element
  tag_name: (tag_name) @name) @item

; Control flow directives
(if_directive) @item

(each_directive) @item

(each_directive_alt) @item

(raw_html_directive) @item
